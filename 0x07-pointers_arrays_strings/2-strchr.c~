#include "main.h"

/**
 *_strchr - locates a character in a string
 *@s: string
 *@c: character
 *Return: c if found and 0 if not
 */

char *_strchr(char *s, char c)
{
while (*s != '\0')
{
if (*s == c)
{
return (s);
break;
}
else
{
return (0);
}
s++;
}
}
