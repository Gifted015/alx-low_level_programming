#include "lists.h"


/**
 *print_listint - prints all the elements of a listint_t list
 *@h: the list to be printed
 *Return: the nuber of nodea
 */

size_t print_listint(const listint_t *h)
{
  listint_t *temp;
  size_t x;

  temp = (listint_t *)h;

  for(x = 1; temp != NULL; x++, temp = temp->next)
    {
      printf("%d\n", temp->n);
    }

  return (x);
}
