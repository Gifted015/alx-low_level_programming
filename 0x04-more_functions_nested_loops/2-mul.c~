#include "main.h"

/**
 *mul - multiplies two integers
 *@a: integer involved in the multiplication
 *@b: integer involved in the multiplication
 *Return: r, the result
 */

int mul(int a, int b)
{
int r(a * b);
return (r);
}
