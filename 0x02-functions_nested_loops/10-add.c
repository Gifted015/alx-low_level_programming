#include <stdio.h>
#include "main.h"
/**
 *add - adds two integers and returns the result.
 *@a: first parameter
 *@b: second parameter
 *Return: r
 */

int add(int a, int b)
{
int r = a + b;
return (r);
}
